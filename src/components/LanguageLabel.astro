---
interface Props {
  contentLang: string;
}

const { contentLang } = Astro.props;
---

<span class="text-[11px] px-1.5 py-0.5 rounded-full bg-black/5 dark:bg-white/5 text-black/40 dark:text-white/40">
  {contentLang === 'en' ? 'EN' : 'CN'}
</span>